import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ez-budget',
  webDir: '../../dist/apps/ez-budget',
  bundledWebRuntime: false,
};

export default config;
